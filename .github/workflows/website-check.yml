name: Website check

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main, development]

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      website: ${{ steps.filter.outputs.website }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          show-progress: false
      - name: Detect website changes
        id: filter
        env:
          EVENT_NAME: ${{ github.event_name }}
          BEFORE_SHA: ${{ github.event.before }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            echo "website=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            compare_from="$BASE_SHA"
          else
            compare_from="$BEFORE_SHA"
          fi

          if [[ -z "$compare_from" ]] || [[ "$compare_from" =~ ^0+$ ]] || ! git cat-file -e "$compare_from^{commit}"; then
            echo "website=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git diff --name-only "$compare_from" "$GITHUB_SHA" > "$RUNNER_TEMP/changed-files.txt"

          if grep -Eq '^(apps/website/|package(-lock)?\.json$|\.github/workflows/website-check\.yml$)' "$RUNNER_TEMP/changed-files.txt"; then
            echo "website=true" >> "$GITHUB_OUTPUT"
          else
            echo "website=false" >> "$GITHUB_OUTPUT"
          fi

  # Always runs: "check" is a required status on main, and a path-skipped
  # job counts as missing, blocking merges of PRs that never touched the
  # website. No website changes → succeed immediately.
  check:
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - name: No website changes
        if: needs.changes.outputs.website != 'true'
        run: echo "No website changes — nothing to check."
      - uses: actions/checkout@v6
        if: needs.changes.outputs.website == 'true'
      - uses: actions/setup-node@v6
        if: needs.changes.outputs.website == 'true'
        with:
          node-version: 22
          cache: npm
      - run: npm ci
        if: needs.changes.outputs.website == 'true'
      - run: npm run check
        if: needs.changes.outputs.website == 'true'
